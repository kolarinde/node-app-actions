name: Deploy Node App

# This tells GitHub exactly when to run the script
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # ... keep your existing deployment steps below this ...